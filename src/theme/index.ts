export { ThemeProvider, useTheme, useThemeContext } from './ThemeProvider';
export { lightTheme } from './light';
export { darkTheme } from './dark';
export type { Theme } from './types';

// Import the styled-components theme declaration
import './styled.d';